snippet     tableTest
abbr        table test template
options     head
    func TestReverse(t *testing.T) {
      cases := []struct {
        in, want {$1}
      }{
        {,},
      }
      for _, c := range cases {
        got := {$2}
        if got != c.want {
          t.Errorf("Reverse(%q) == %q, want %q", c.in, got, c.want)
        }
      }
    }
